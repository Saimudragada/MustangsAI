# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration (Cost Optimization)
# Embedding Model - text-embedding-3-small is cost-effective ($0.02/1M tokens)
OPENAI_EMBED_MODEL=text-embedding-3-small

# Chat Model Options:
# - gpt-4o-mini: $0.15/1M input, $0.60/1M output (RECOMMENDED for budget)
# - gpt-4o: $2.50/1M input, $10/1M output (higher quality)
# - gpt-3.5-turbo: $0.50/1M input, $1.50/1M output (legacy)
OPENAI_CHAT_MODEL=gpt-4o-mini

# Alternative: Use Claude API (Anthropic)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# CHAT_MODEL=claude-sonnet-3.5

# Rate Limiting (optional - for production deployment)
# MAX_QUERIES_PER_DAY=100
# MAX_QUERIES_PER_USER=10

# Deployment (optional)
# PORT=8501
# STREAMLIT_SERVER_HEADLESS=true
# STREAMLIT_SERVER_ENABLE_CORS=false
